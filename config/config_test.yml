imports:
    - { resource: config_dev.yml }

parameters:
    ldap_client.class: App\Ldap\MockClient

framework:
    test: ~
    session:
        storage_id: session.storage.mock_file

web_profiler:
    toolbar: false
    intercept_redirects: false